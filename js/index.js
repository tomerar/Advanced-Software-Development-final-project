
$(document).ready(function() {
        var firebase_init = new FirebaseInit(); 
        // firebase_init.is_login(null,"login.html")
        firebase_init.is_login(null,"login.html")
          console.log("wowo");
          
});

