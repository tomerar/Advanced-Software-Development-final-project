$(document).ready(function() {
  var firebase_init = new FirebaseInit();

  firebase_init.is_login("index.html",null)
  // firebase_init.is_login(null,null)
  var login_tools = new LoginTools();

});

